extends layout.pug
    
block content
    #outerMap    
        h1 Outer map
        
        script(src='/scripts/script_Gps.js')
        
        // Pug does not parse this properly
        //script(src="https://maps.googleapis.com/maps/api/js?key=#{settings.MAPKEY}&callback=initMap" async defer)
        
        // Pug parses this properly
        <script src="https://maps.googleapis.com/maps/api/js?key=#{settings.MAPKEY}&callback=initMap" async defer></script>
        
        
        #map
            
    style(type='text/css').
        body {
            height: 100%; 
            margin: 0; 
            padding: 0;}
        
        #map {
            width: 300px;
            height: 300px;
            display: block;}        